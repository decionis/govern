{
  "name": "@decionis/github-action",
  "version": "0.1.0",
  "description": "Govern a GitHub Actions step on a Decionis Decision Dossier — gate deploys, releases, infra changes, and agent-triggered actions on a signed verdict.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "eslint src test",
    "format": "prettier --check . --ignore-path ../../.prettierignore",
    "format:fix": "prettier --write . --ignore-path ../../.prettierignore"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "action.yml",
    "src",
    "README.md"
  ],
  "license": "UNLICENSED",
  "homepage": "https://decionis.com/marketplace/github"
}
